spring.application.name=library-system
# Database URL (adjust port/db if different)
spring.datasource.url=jdbc:postgresql://localhost:5432/library_system

# Your PostgreSQL credentials
spring.datasource.username=postgres
spring.datasource.password=ra1381zaq

# Optional but useful configs
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect